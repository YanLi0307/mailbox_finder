%h1 Mailbox Finder!
= form_tag mailboxes_path, :method => :get do
  %p
    = text_field_tag :search, params[:search]
    = submit_tag "Search Near", { class: "medium button", name: nil }
.row
  = gmaps4rails(@json)

%a= link_to "Add New Mailbox", new_mailbox_path, { class: "medium button radius" }

%h2 Mailboxes

%ul
  - @mailboxes.each do |mailbox|
    %li= link_to mailbox.location, mailbox
%a= link_to "Add New Mailbox", new_mailbox_path, { class: "medium button" }
